<div class="container mx-auto">
    <h2 class="text-3xl font-bold text-gray-800 mb-6">Beérkezett Üzenetek</h2>
    <p class="text-gray-600 mb-6">Itt láthatók a kapcsolat űrlapon küldött üzenetek, időrendben.</p>

    <div class="grid gap-6">
        {{#each messages}}
            <div class="bg-white p-6 rounded-lg shadow border-l-4 border-indigo-500">
                <div class="flex justify-between items-start mb-2">
                    <div>
                        <h3 class="font-bold text-lg text-gray-800">{{this.sender_name}}</h3>
                        <span class="text-sm text-gray-500">{{this.email}}</span>
                    </div>
                    <div class="text-xs bg-gray-200 px-2 py-1 rounded text-gray-600">
                        {{formatDate this.created_at}}
                    </div>
                </div>
                <p class="text-gray-700 mt-3 bg-gray-50 p-3 rounded">{{this.message}}</p>
            </div>
        {{else}}
            <p class="text-center text-gray-500 py-10">Nincs új üzenet.</p>
        {{/each}}
    </div>
</div>